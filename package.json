{
  "name": "@buildbutler/github-action",
  "version": "1.0.0",
  "private": true,
  "description": "Build Butler GitHub Actions integration",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && node esbuild.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "fast-xml-parser": "^4.5.0",
    "glob": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
